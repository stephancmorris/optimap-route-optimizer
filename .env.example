# Backend Configuration
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# OSRM API Configuration
OSRM_BASE_URL=http://router.project-osrm.org

# OR-Tools Solver Configuration
SOLVER_TIME_LIMIT_SECONDS=30

# CORS Configuration
# Comma-separated list of allowed origins for cross-origin requests
# Development: Use localhost with frontend ports (Vite: 5173, Production build: 3000)
# Production: Replace with your actual domain(s), e.g., https://yourdomain.com
# IMPORTANT: Never use "*" in production - always specify exact origins
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Logging
LOG_LEVEL=INFO
